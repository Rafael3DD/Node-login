
<h2>Editar Usuario:</h2>

<hr>

{{#each erros}} <!--erros--> 
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}


{{#each usuarios}} <!--erros--> 

    <div class="card mt-4">
        <div id="admin">
               {{#if eAdmin}}
                    admin: <strong>sim </strong>
                {{else}}
                    admin: <strong>não </strong>
                {{/if}}
        </div>

        <div class="card-body" id="usuario">
            <h4>{{this.nome}}</h4> <!--Mostra o nome da categoria-->
            <small><strong>Nome:</strong> {{nome}}</small>
            <br>
            <small><strong>Email:</strong> {{email}}</small> <br>
            <a href="/admin/usuario/edit/{{_id}}"><button class="btn btn-success btn-sm mt-4">Editar Usuario</button></a>
            
            <form action="/admin/usuario/deletar" method="POST">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-danger mt-2 btn-sm">Apagar Usuario</button>
            </form>
        </div>

    </div>
    
{{else}}

{{/each}}