
            <div class="row no-gutters justify-content-center text-center">
                <table class="table mt-2" id="tabela">
                    <thead>
                        <tr>
                            <th scope="col">Nome</th>
                            <th scope="col">E-mail</th>
                            <th scope="col">É admin</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each usuarios}}
                        <tr>
                            <td>{{nome}}</td>
                            <td>{{email}}</td>
                            <td>{{#if eAdmin}}
                                admin: <strong>sim </strong>
                                {{else}}
                                admin: <strong>não </strong>
                                {{/if}}
                            </td>

                            
                            <td><a href="/admin/usuario/edit/{{_id}}"><button class="btn btn-secondary btn-sm ">Editar</button></a>
                            </div >

                            <div id="div-delete">
                            <form action="/admin/usuario/deletar" method="POST">
                                    <input type="hidden" name="id" value="{{_id}}">
                                    <button type="submit" class="btn btn-danger btn-sm" id="btn-editUsuario"><i class="fa-solid fa-trash-can"></i></button>
                            </form> 
                            </td>
                            </div>

                            {{else}}
                            {{/each}}
                        </tr>
                    </tbody>
                </table>
            </div>